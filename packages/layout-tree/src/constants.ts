export enum LegacyOrientation {
  Horizontal = "horizontal",
  Vertical = "vertical",
}

export type Orientation = "vertical" | "horizontal";

export enum Direction {
  Up,
  Down,
  Left,
  Right,
}
